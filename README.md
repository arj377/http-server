# HTTP Server

A multithreaded HTTP/1.1 server written in C++.

## Features
- Static file serving
- Thread pool concurrency
- HTTP request parsing
- Persistent connections (planned)

## Build

```bash
mkdir build
cd build
cmake ..
make
```

## Run

```bash
./http_server
```